apiVersion: v1
kind: Pod
metadata:
  name: postgres-pod
  labels:
    app: postgres-app
    name: postgres-pod
spec:
  containers:
  - name: postgres-container
    image: postgres
    ports:
    - containerPort: 5432