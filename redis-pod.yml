apiVersion: v1
kind: Pod
metadata:
  name: redis-pod
  labels:
    app: redis-app
    name: redis-pod
spec:
  containers:
  - name: redis-container
    image: redis
    ports:
    - containerPort: 6379


